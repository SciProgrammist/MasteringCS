/**
 * Math y Date
 */

/**
 * ramdon() this function give us a ramdon nummber between 0 and 1
 * We can scale the factor by any number we need.
 */
// 0 - 1
const valor = Math.random();
const v = 10;
// 0 - 10 
const valorScaled = Math.random()*v;
console.log(valorScaled);

/**
 * . accessing an attribute
 * 
 */


const value = Math.PI;
console.log(value);

const test = Math.LN2
console.log(test);

/**
 * . accessing a method
 * 
 */

const valore = Math.cos(Math.PI);
console.log(valore)

/**
 * DATE
 * accessing its functionality.
 * 
 */

const valori = new Date;
console.log(valori)
console.log(valori.getFullYear());
